{
  "version": 1,
  "canisters": {
    "backend": {
      "type": "rust",
      "package": "onchain360_backend",
      "root": "backend",
      "candid": "backend/backend.did",
      "build": "cargo build --target wasm32-unknown-unknown --release --manifest-path backend/Cargo.toml"
    },
    "frontend": {
      "type": "assets",
      "source": ["frontend/public"],
      "build": "echo 'Building frontend...' && exit 0"
    }
  },
  "networks": {
    "local": {
      "bind": "127.0.0.1:8000",
      "type": "ephemeral"
    },
    "ic": {
      "providers": ["https://ic0.app"]
    }
  },
  "defaults": {
    "build": {
      "packtool": "esbuild"
    },
    "network": "local"
  }
}